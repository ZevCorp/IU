# HRM Service - Dependencies for Jetson Orin Nano
# 
# IMPORTANT: PyTorch should be installed from NVIDIA's Jetson wheels:
#   pip3 install torch torchvision --extra-index-url https://developer.download.nvidia.com/compute/pytorch/whl/cu118
#
# HRM repository (https://github.com/sapientinc/HRM) should be CLONED, not pip-installed.
# Add it to PYTHONPATH before running:
#   export PYTHONPATH="${PYTHONPATH}:$(pwd)/HRM"

# ========================================
# HRM Core Dependencies (from HRM/requirements.txt)
# ========================================
adam-atan2>=0.0.1
einops>=0.7.0
tqdm>=4.66.0
coolname>=2.2.0
pydantic>=2.0.0
argdantic>=1.0.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# ========================================
# HuggingFace (for model checkpoint download)
# ========================================
huggingface_hub>=0.20.0

# ========================================
# WebSocket Client
# ========================================
websockets>=12.0

# ========================================
# Optional: FlashAttention (recommended for GPU performance)
# Note: May require building from source on ARM64
# ========================================
# flash-attn  # Uncomment if available for your platform

# ========================================
# YAML parsing
# ========================================
PyYAML>=6.0

# ========================================
# Weights & Biases (optional, for training/logging)
# ========================================
# wandb  # Uncomment if needed for experiment tracking
